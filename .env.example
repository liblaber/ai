# Client public env vars
NEXT_PUBLIC_DISABLE_TELEMETRY=false # Disable telemetry collection for analytics
NEXT_PUBLIC_ENV_NAME=local # Environment name for client-side configuration
NEXT_PUBLIC_LOG_LEVEL=debug # Logging level for client-side debugging
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com # PostHog analytics host URL
NEXT_PUBLIC_POSTHOG_KEY=phc_C1L7Idffoa2oRT54XZDGh4GSdOPFgzliJiusFnVV8Wz # PostHog project API key
NEXT_PUBLIC_TUNNEL_FORWARDING_URL=https://your-tunnel-url.ngrok-free.app # Tunnel URL for local development

# General
BASE_URL=http://localhost:3000 # Base URL for the application
ENCRYPTION_KEY=your-32-byte-base64-encoded-encryption-key # 32-byte base64 encoded encryption key for data security
LICENSE_KEY=free # License tier (free/paid)
STARTER=next # Starter template type
STORAGE_TYPE=FILE_SYSTEM # Storage backend type (FILE_SYSTEM/DATABASE)
VITE_API_BASE_URL=http://localhost:3000 # Vite API base URL for frontend
VITE_BASE_URL=http://localhost:3000 # Vite base URL for frontend

# Available providers: AmazonBedrock, Anthropic, Cohere, Deepseek, Github, Google, Groq, HuggingFace, Hyperbolic, LMStudio, Mistral, Ollama, OpenAI, OpenAILike, OpenRouter, Perplexity, Together, xAI
DEFAULT_LLM_PROVIDER=Anthropic # Default LLM provider for AI operations
DEFAULT_LLM_MODEL=claude-4-sonnet-20250514 # Default AI model to use

# Get your Anthropic API Key in your account settings - https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY= # API key for Anthropic Claude models

# Optionally, you can set the OpenRouter API key if you use OpenRouter as a provider
# OPEN_ROUTER_API_KEY= # API key for OpenRouter service (optional)

# Optionally, you can set the Ollama API base URL if you use Ollama as a provider
# OLLAMA_API_BASE_URL=http://localhost:11434 # Base URL for local Ollama instance (optional)

DATABASE_URL=postgresql://username:password@localhost:5432/database_name # PostgreSQL database connection string
AUTH_SECRET=your-44-character-auth-secret-string-here # Secret key for authentication/session management

# Provide the Netlify token to enable app deployment
NETLIFY_AUTH_TOKEN= # Netlify API token for deployment automation
